# Data

`data/raw/` contains the CSV inputs used by the training scripts.

Supported naming conventions:

- `<target>_<fid>.csv`
- `<fid>/<target>.csv`

Keep generated artifacts, train/val/test splits, and result folders outside `data/raw/`.
